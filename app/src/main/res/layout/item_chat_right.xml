<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingTop="10dp"
    android:paddingBottom="10dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:src="@drawable/bg"
        android:id="@+id/iv_right_avatar"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_width="40dp"
        android:layout_height="40dp" />

    <TextView
        android:background="@drawable/bg_chat_content"
        android:id="@+id/tv_right_content"
        android:paddingLeft="8dp"
        android:paddingRight="8dp"
        android:text="1111111"
        android:gravity="center_vertical"
        android:textSize="18sp"
        app:layout_constraintRight_toLeftOf="@id/iv_right_avatar"
        app:layout_constraintTop_toTopOf="@id/iv_right_avatar"
        app:layout_constraintBottom_toBottomOf="@id/iv_right_avatar"
        android:layout_marginRight="10dp"
        android:layout_width="wrap_content"
        android:layout_height="0dp" />
</androidx.constraintlayout.widget.ConstraintLayout>